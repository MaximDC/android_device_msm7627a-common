From 968bd46e94d968f09d86ebd5156dcc6efdc1e401 Mon Sep 17 00:00:00 2001
From: techyzen101 <techyzen101@gmail.com>
Date: Fri, 15 Apr 2016 17:30:13 +0800
Subject: [PATCH 1/4] Offline Charging

---
 init/Android.mk |  4 +++-
 init/init.cpp   | 12 ++++++++++--
 2 files changed, 13 insertions(+), 3 deletions(-)

diff --git a/init/Android.mk b/init/Android.mk
index d66ac5a..367604b 100644
--- a/init/Android.mk
+++ b/init/Android.mk
@@ -83,7 +83,9 @@ LOCAL_SRC_FILES:= \
 
 SYSTEM_CORE_INIT_DEFINES := BOARD_CHARGING_MODE_BOOTING_LPM \
     BOARD_CHARGING_CMDLINE_NAME \
-    BOARD_CHARGING_CMDLINE_VALUE
+    BOARD_CHARGING_CMDLINE_VALUE \
+    BOARD_LPM_BOOT_ARGUMENT_NAME \
+    BOARD_LPM_BOOT_ARGUMENT_VALUE
 
 $(foreach system_core_init_define,$(SYSTEM_CORE_INIT_DEFINES), \
   $(if $($(system_core_init_define)), \
diff --git a/init/init.cpp b/init/init.cpp
index 61f4b7c..059ca13 100755
--- a/init/init.cpp
+++ b/init/init.cpp
@@ -91,6 +91,8 @@ bool waiting_for_exec = false;
 
 static int epoll_fd = -1;
 
+static unsigned lpm_bootmode = 0;
+
 void register_epoll_handler(int fd, void (*fn)()) {
     epoll_event ev;
     ev.events = EPOLLIN;
@@ -529,11 +531,12 @@ static void selinux_initialize(bool in_kernel_domain) {
     } else {
         selinux_init_all_handles();
     }
+ 
 }
 
 static int charging_mode_booting(void) {
 #ifndef BOARD_CHARGING_MODE_BOOTING_LPM
-    return 0;
+    return lpm_bootmode;
 #else
     int f;
     char cmb;
@@ -675,7 +678,12 @@ int main(int argc, char** argv) {
     if (bootmode == "charger" || charging_mode_booting() ||
             strcmp(battchg_pause, BOARD_CHARGING_CMDLINE_VALUE) == 0) {
         am.QueueEventTrigger("charger");
-    } else if (strncmp(bootmode.c_str(), "ffbm", 4) == 0) {
+    } 
+	else if (!strcmp(name,BOARD_LPM_BOOT_ARGUMENT_NAME)) {
+        if (!strcmp(value,BOARD_LPM_BOOT_ARGUMENT_VALUE)) {
+	lpm_bootmode = 1;
+	
+	else if (strncmp(bootmode.c_str(), "ffbm", 4) == 0) {
         NOTICE("Booting into ffbm mode\n");
         am.QueueEventTrigger("ffbm");
     } else {
-- 
2.10.1

